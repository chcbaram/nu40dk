# 저전력 동작용 오버레이.
#
#   west build ... -- -DEXTRA_CONF_FILE=low_power.conf
#   (nRF Connect 확장에서는 빌드 설정의 Kconfig fragment 에 추가)
#
# CLI 를 빼면 USB 디바이스 스택, UART 드라이버, CLI 코드가 모두 빌드에서 제외된다.
#
CONFIG_APP_USE_CLI=n

CONFIG_DEBUG_OPTIMIZATIONS=n
CONFIG_DEBUG_THREAD_INFO=n
CONFIG_SIZE_OPTIMIZATIONS=y
